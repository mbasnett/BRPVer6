global class MasterdataPull implements Schedulable
{
   global void execute(SchedulableContext SC) 
   { 
   
    PullNewstarGeographyLayoutBatch b=new PullNewstarGeographyLayoutBatch();
    Database.executeBatch(b);

   }
     
}